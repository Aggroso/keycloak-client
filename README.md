# keycloak-client
Multi-app auth helpers: Python and TypeScript clients that integrate with Keycloak by calling your OIDC BFF (login, callback, refresh, logout, optional userinfo). Keeps protocol logic on the server; frontends and services use one stable contract.
